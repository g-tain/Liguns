/**
 * src/hooks/index.ts
 *
 * Hooks barrel export.
 * Add custom hooks here as the app grows.
 *
 * Example (Phase 2):
 * export { useUser } from "./useUser"
 * export { useSupabase } from "./useSupabase"
 * export { useScrollProgress } from "./useScrollProgress"
 */
